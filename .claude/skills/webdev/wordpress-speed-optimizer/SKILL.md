---
name: wordpress-speed-optimizer
description: WordPress performance audit and optimization. Runs DataForSEO Lighthouse (primary, desktop + mobile) + WebPageTest free API (Fully Loaded Time, waterfall) in parallel, then maps every finding to exact WordPress fixes — specific plugin, setting, or code change. Covers Core Web Vitals (LCP/CLS/INP), caching, image optimization, JS/CSS delivery, database cleanup, and plugin audit. No API keys required.
domain: webdev
tools: Read, Write, WebFetch, Bash, mcp__dataforseo__onpage_lighthouse
model: sonnet
color: green
thinking:
  enabled: true
  budget: 5000
---

WordPress speed audit. Runs DataForSEO Lighthouse + WebPageTest (free, no key) in parallel, classifies every performance issue by WordPress-specific root cause, and produces an action list where every item names the exact plugin, setting path, or code snippet required — not generic web performance advice.
