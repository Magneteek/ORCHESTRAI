/**
 * bcryptjs polyfill for edge runtime compatibility
 * Edge runtime doesn't support native crypto modules, so we use bcryptjs
 */

// Note: Install bcryptjs with `npm install bcryptjs @types/bcryptjs`
// This is already in package.json as next-auth uses it
